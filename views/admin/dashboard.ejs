<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@600&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #a6d4d8, #323e4a);
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .dashboard-container {
      background: rgba(255, 255, 255, 0.);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      padding: 60px 50px;
      border-radius: 30px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      text-align: center;
      width: 95%;
      max-width: 950px;
    }

    h1 {
      font-family: 'Rajdhani', sans-serif;
      font-size: 48px;
      color: #2c3e50;
      margin-bottom: 15px;
    }

    p {
      font-size: 20px;
      color: #555;
      margin-bottom: 40px;
    }

    .btn-glass {
      background: rgba(255, 255, 255, 0.25);
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
      border: 1px solid rgba(0, 0, 0, 0.1);
      color: #333;
      font-weight: 600;
      font-size: 18px;
      padding: 16px;
      border-radius: 14px;
      transition: all 0.3s ease-in-out;
    }

    .btn-glass:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.2);
    }

    .row-cols-md-3 .col {
      min-width: 240px;
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <h1>Admin Dashboard</h1>
    <p>Select a table to manage</p>

    <div class="row row-cols-1 row-cols-md-3 g-4 justify-content-center">
      <div class="col">
        <a href="/admin/users" class="btn btn-glass w-100">👤 Manage Users</a>
      </div>
      <div class="col">
        <a href="/admin/stations" class="btn btn-glass w-100">🏙️ Manage Stations</a>
      </div>
      <div class="col">
        <a href="/admin/trains" class="btn btn-glass w-100">🚆 Manage Trains</a>
      </div>
      <div class="col">
        <a href="/admin/routes" class="btn btn-glass w-100">🗺️ Manage Routes</a>
      </div>
      <div class="col">
        <a href="/admin/schedules" class="btn btn-glass w-100">⏱️ Manage Schedules</a>
      </div>
      <div class="col">
        <a href="/admin/bookings" class="btn btn-glass w-100">📄 Manage Bookings</a>
      </div>
    </div>
    <br>
    <div class="col">
      <a href="/admin/er-diagram" class="btn btn-glass w-100">📄 View ER-diagram</a>
    </div>
  </div>
</body>
</html>
